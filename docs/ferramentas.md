# Ferramentas

SEFAZ PB: Certidão Negativa de Débitos expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_pb_certidao_debitos_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `ie` (opcional), `identidade_numero` (opcional), `identidade_emissor_uf` (opcional), `identidade_emissor_orgao` (opcional)

SEFAZ PB: Certidão Negativa de Débitos, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ PB: Certidão Negativa de Débitos
```
